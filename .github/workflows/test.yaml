name: Test exercises

on:
  pull_request:
    branches:
      - main

jobs:
  test:
    steps:
      - uses: action/checkout@v4

      - name: Test exercises
        run: find . -name go.mod -execdir sh -c 'go test ./... | tee /dev/stderr | grep -q FAIL && exit 1' \;
